import { routeConfig,defaultRedirects,authUrls } from "./routes.config";
import { intlConfig } from "./intl.config";
export const configs = {
    routeConfig,
    defaultRedirects,
    authUrls,
    intlConfig
};